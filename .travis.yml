language: java
jdk:
  - oraclejdk8
  
before_install:
  - chmod +x mvnw
  - mysql -u root -p123456 -e "CREATE USER 'travis'@'localhost' identified by '';"  
  
test:
  adapter: mysql2
  database: ponto_inteligente
  username: travis
  encoding: utf8
  
dist: 
  precise